# 全ページ展開・Claude移管チェックポイント（未完成）

2026-09-10。ファウンダーが代表3画面の方向と全ページ展開を承認。その後「区切りでClaudeへ渡す最終パックの保存を優先、未完了を明記」と指示。ここで実装修正を停止し、受け渡しを行う。本番公開の完成判定ではない。

## 実装と範囲

基点2f4a156890e0714816624441b8dbbcbf8feede0d、ブランチcodex/design-redesign-20260910。代表3画面の実装を保持し、共通CSS、生成モジュール、page-chrome、公開HTMLへ展開。143 HTML＝当初の公開面138＋現行の作品ページから到達するculture-experience-r2の5 HTML。sitemap125 URLとは定義が違う（noindexの保存・補助頁、パラメータ、現在リンクされる体験頁などを含む）。全ファイル一覧はqa/design-redesign/routes.json。古いarchive、無関係なprototype、experiments、QA基準画面は対象外。Event Preflightのコードは扱っていない。

生成元の対応：discover/88頁はtools/build-city-discovery.js、outings/35頁はtools/build-weekly-outings.js（city生成から呼ばれる）、works＋work-4頁はtools/build-work-pages.js。いずれもtools/page-chrome.jsからtools/design-redesign.jsへ接続。残る手書きページと既存の体験5頁はtools/build-design-redesign.jsで適用・冪等性検査。

白背景、本文#222522、補助文字#565d56、緑#285b43、線#d9ded8。見出しは既存EB Display、本文と操作は日本語sans-serif。一覧2列・760px以下1列、長文幅46rem（記事本文42rem）、44px操作目標。新規外部画像・フォント・依存は追加していない。催しヘッダーは既存ロゴ画像を再使用。

## 検証とその限界

- node qa/verify-product.js：全ページ展開途中のログv2/v3で20/20 PASS。最終SHAの再検査結果は受渡しパックQAへ別記。
- node qa/design_redesign_check.js：143頁のテーマ、冪等性、本文・リンク/メディア先保持、protected runtimeの一致を確認。比較基点は2f4a156。CSSと既存ブランド画像の追加を許容し、それ以外のリンク・画像・iframe等の参照は完全一致で検査。
- node tools/build-design-redesign.js --check：143頁の適用一致PASS。
- Chromium、同一origin iframe：24画面×6幅＝144測定で横はみ出し・読み込み済み破損画像を検出せず。その後作品・催し・音映像8画面×6幅＝48測定を実施。映画体験320pxの枠はみ出しを検出し修正、再測定48件で検出なし。合計192測定、重複を除く29代表画面×6幅＝174組。143頁すべての目視検査ではない。lazy load未完了画像の正常性はこの結果で保証しない。
- ブラウザ実操作：合成メモ＋スタンプ→保存一覧1件→検索0件→解除0件。実データは使用せず合成データのみ解除。催しの吉祥寺絞り込み3件→秋まつり詳細→週・街の条件を保って一覧へ戻る。記事一覧から下北沢記事へのEnter操作で遷移成功。
- 白背景に対する基本色の計算比：本文15.49、補助6.78、緑7.87。全状態の配色監査ではない。
- PARKS埋め込みのPlayを操作したが、取得した状態は0:00/0:00で、進行する再生時刻を確認していない。再生成功とはしない。

## Claudeが続ける未完了

1. 催しヘッダーで画像ロゴ直下にブランド名が重複表示。追加したsr-onlyに対象画面のスタイル定義が不足。最新outings画像で確認。視覚的な重複を直しアクセシブル名を保持する。
2. 最終CSS変更後の全代表画面の目視確認、フォント代替・低コントラスト状態の確認。機械的なoverflow合格と視覚的完成を区別する。特に聴く/観るページは最後に追加したため確認が薄い。
3. Safari/WebKit、実機Safari、実際の200%ブラウザズーム、reduced-motion、全キーボード/フォーカス、長い作品名・画像なし・空状態の代表検証。前段のroot font-size200%はブラウザ200%ズームではなく、現SHAの全ページ証拠ではない。
4. 動画の実再生、音声の実再生、5主要導線の完結検証、保存後の再訪を実施。実施済み部分から再開する。
5. 同条件の性能before/after、残る参照サイトの一覧/詳細/モバイル比較。方向性の比較を最初からやり直さず、品質に必要な範囲へ限定する。
6. 内部QAのbefore選択は最初の3画面のみ実ファイルあり。他画面でbeforeを選ぶと比較先が存在しない。製品ではなく検証ハーネスの未完了。追加幅検査はload timeoutを明示し、操作中の選択変更を避ける。
7. 本番/Previewの正確なデプロイSHAを未確認。前段のVercel接続はプロジェクト一覧空・個別404。今回push/PR/merge/deployは未実施。mainとの統合差分には以前の未公開3コミットも含むため、公開範囲を照合する。

## 次の一手

同梱bundleを別ディレクトリへ復元しSHAとハッシュを照合→残件1の限定修正→最終画面・操作の検証→公開対象と正規公開手順の照合。未完成の証拠を合格へ変更しない。ファウンダーの保存優先指示により、ChatGPT側の追加編集はこのチェックポイントで停止する。
