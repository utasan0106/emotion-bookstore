# AI社員運用 — ops index

このディレクトリはAI社員運用のPoC/設計文書を置く場所。product diffには含めない。

- `poc-001/` — PoC v0.1 実地記録（`README_AI_EMPLOYEE_POC.md`, `POC_TASK_001_SINGLE_BOOK_DELETE_CONFIRM.md`）
- 運用ルール本体は `CLAUDE.md`（不変条件）と `.claude/agents/`・`.claude/skills/`（手順）を参照。
