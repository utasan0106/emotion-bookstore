# Visual Refit V2 — 静的UIシェル（プロトタイプ）

本番UIから物理的に隔離した静的シェル。旧 `index.html` / `main.js` / `data.js` / `style.css`
には接続せず、旧CSSを import せず、旧DOM・旧セレクタも再利用しない。

Visual North Star は正式カンプ `FORMAL_COMPS/DAY/*.png` ・ `FORMAL_COMPS/NIGHT/*.png` のみ。
現行サイト・旧UI・旧VR1 の見た目は視覚の参照にしていない。

## 現在の状態

| 画面 | 状態 |
|---|---|
| 01 表紙 | **Structural Skeleton 実装済み**（Visual QA 未実施） |
| 02〜08 | 未着手 |

**01 はまだ完成ではない。** 素材未確定部分を無地面・輪郭のプレースホルダーで置いた
Structural Skeleton であり、以下の確認にのみ使用する。

- DOM構造
- 画面比率
- 要素位置
- 占有面積
- 余白
- 情報階層
- DAY/NIGHT 共通構造

この状態を「01完成」「Visual QA合格」「正式カンプ一致」「次画面へ進める根拠」として扱わない。
正式な Visual QA は必要素材が揃った後に別途行う。

## ファイル構成

```
visual-refit-v2-prototype/
├ index.html              画面索引
├ screens/01-cover.html   01 表紙（DAY/NIGHT 同一DOM）
├ css/tokens.css          V2専用トークン（DAY/NIGHT）
├ css/base.css            リセット・書体・ステージ原型
├ css/components.css      .v2-btn / .v2-navbar / .v2-rule
├ css/screens/01.css      01 固有
└ README.md               本書
```

全クラスに `v2-` プレフィックスを付け、旧 `style.css` と名前空間が交差しないようにしている。

## 昼夜の切り替え

`:root[data-v2-theme="day" | "night"]` のみで切り替える。DAY/NIGHT は同一DOM。
既存本番の `body.night` / `data-daypart` / `applyNightModeIfNeeded()` / `currentDaypart()`
には触れていない（保護領域）。

`screens/01-cover.html` には Visual QA 専用の最小インラインJSが1本だけ入っている。
URLパラメータ `theme=day|night` を読み `data-v2-theme` を差し替えるだけで、
保存しない／localStorage を使わない／GA4 を呼ばない／外部通信しない／時刻判定しない。
**Phase 3 の本番Adapter接続時には、この処理を本番ロジックへ持ち込まない。**

## アクセントの役割分担（CEO決定 2026-08-09）

| トークン | 用途 | DAY | NIGHT |
|---|---|---|---|
| `--v2-accent-place` | 書店側・棚を見る・環境的な現在地 | 金茶 | 金〜琥珀 |
| `--v2-accent-self` | 自分の記録・主要操作・選択状態・読書操作 | 低彩度の青 | 金〜琥珀 |

01 の「入店する」は書店側の体験への入場のため `--v2-accent-place`（金茶）。右矢印は付けない。

## 01 の寸法契約（正式カンプ実測値 / 853×1844 基準）

| 要素 | カンプ実測 |
|---|---|
| 画面比 | 853 × 1844（1:2.161） |
| 壁面／アーチ境界 | x 62.8%（y≥35% は垂直）、上部はアーチ頭で右へ抜ける |
| ロゴ | y 296–482 / x 118–295 |
| 題字1行目「みんなの」 | y 517–565 / x 91–329 |
| 題字2行目「感情書店」 | y 602–676 / x 88–449 |
| 罫 | y 739–742 / x 88–145 |
| リード2行 | y 791–869 / x 88–393 |
| 棚板・本の物体 | y 1009–1374 / x 0–500 |
| 主CTA | y 1470–1600 / x 128–722 |
| 下部4ナビ | アイコン y 1678–1722 ／ ラベル y 1746–1770 |
| ナビ4項目の中心 | 15.8% / 38.3% / 60.8% / 83.3% |

## プレースホルダー（素材未確定）

無地面＋破線の輪郭のみ。絵柄を推測しない。
**カンプ画像の切り抜き・トレース・生成画像・ランダム素材は使用していない。**

| 箇所 | 現状 |
|---|---|
| 壁面 | トークン色＋縦方向の明暗のみ |
| アーチ内の店内 | 無地面＋輪郭。光は明暗の勾配のみで表現し、照明器具は置かない |
| 葉影 | 面積・位置を示す輪郭のみ |
| 棚板・本の物体 | 面積・位置を示す輪郭のみ |
| ロゴ | 円形の中立プレースホルダー。`assets/shop-seal.png` の加工・透過化による代用はしない |
| ナビアイコン4種 | 正方形の輪郭のみ |

## 未接続・非活性

- 主CTA … `disabled` + `aria-disabled="true"`。外観はカンプの通常状態を再現（視覚的な disabled 表現にしない）
- 下部ナビ … 視覚要素のみ。`href` / `onclick` / route を持たない
- 「わたしの本」「わたし」「お店へ」のための新 route は作らない（Phase 3 Adapter 接続前に対応表を別途決定）
- 保存 / IndexedDB / localStorage / GA4 / 外部通信 … 一切行わない

## Visual QA の撮影条件

| 画面 | viewport | DPR | 出力 |
|---|---|---|---|
| 01 | 427 × 922 | 2 | 854 × 1844（カンプ 853 × 1844） |
| 02〜07 | 512 × 768 | 2 | 1024 × 1536 |
| 08 | 561 × 701 | 2 | 1122 × 1402 |

01 と 08 は他画面と原寸比が異なるため、1024×1536 に矯正しない。
