# 候補の受け取りと、毎週の見直し

「候補を教える」で集まったものが Google Drive に自動で届き、Claude が毎週
確認して更新案を出すまでの手順。運用文書であって、配信物ではない
（`qa/` は `.vercelignore` で配信面から除外されている）。

## いまの設計と、変えなければいけないこと

`suggest.html` は意図的に backend を持たない。ページの約束はこう書いてある。

> このページでは保存・計測・個人ごとの推薦を行いません。入力内容は自動送信
> されません。「候補文をコピー」を押した場合だけ、端末のクリップボードに
> コピーされます。公式Xは押したときだけ開きます。

この一文は RC02 の独立監査で確定した文言で、`qa/release_check.js` が
一字一句で守っている。つまり「自動で届く」を実現するには、この約束と
正面から向き合う必要がある。黙って backend を足すことはできない。

## 採る方式: Google フォームへ出るリンク

送信は **Google のページ側で** 起こし、`suggest.html` 自体は何も送らない。

- 利用者が「フォームで送る」を押したときだけ、Google フォームが開く
- 記入も送信も Google のページで行われる
- 回答は Founder の Drive にあるスプレッドシートへ自動で入る
- `suggest.html` からの外部送信は、これまで通りゼロ

構造としては、すでに監査を通っている公式Xのリンクと同じ
（押したときだけ外部が開く）。だから約束を破らずに、開示を1文足すだけで済む。

backend や第三者フォームサービスを置く案は採らない。secret の管理が要り、
ページの性格そのものが変わり、`YOU MUST NOT add external data transmission`
の趣旨からも遠い。

## 現在の状態（2026-08-29）

フォームは作成・公開済み。`suggest.html` に「送信フォームを開く」を追加し、
開示文も差し替えた。貼っている URL はこれ。

```
https://docs.google.com/forms/d/e/1FAIpQLSdzTmEy7PyBOa3XdGU375tib6dK7PHHa5XMGy1Aw3w8v5aa6w/viewform
```

匿名の訪問者から開けることを、シークレットウィンドウで確認済み。

ここに至るまでに2回間違えている。記録として残す。

1. Founder から最初に渡されたのは編集用の `/edit` URL だった。公開ページに
   貼ると訪問者がフォームそのものを書き換えられる。閲覧用に直した。
2. 直した `/d/<id>/viewform` も、匿名では「このドキュメントは公開されて
   いません。」になった。原因はフォームが未公開だったこと。公開して発行される
   `/d/e/1FAIpQLS…/viewform` が、他人が記入するための正しい URL。

どちらも静的検査では気づけず、シークレットウィンドウで開いて初めて分かった。
URL を差し替えたときは、必ず匿名で一度開く。

受け取る URL には `?usp=publish-editor` や `?usp=dialog` が付いてくることが
あるが、これは編集画面・共有ダイアログから来たことを示す印なので外す。
`release_check.js` が query 付きを FAIL にするため、外した形でしか通らない。

## フォーム側の設定（2026-08-29 完了・確認済み）

| 項目 | 値 |
|---|---|
| タイトル | みんなの感情書店｜候補を教える |
| メールアドレスを収集する | 収集しない |
| 回答のコピーを回答者に送信 | オフ |
| 回答の編集を許可する | オフ |
| 回答を1回に制限する | オフ（ログインを強制しない） |

メールを集めないのは、集めていない情報は漏らしようがないから。返信もしない
前提なので、フォームの説明にも「個別の返信はできません」と書いてある。
「1回に制限する」をオフにしているのは、オンだと Google ログインが必須になり、
ふらっと来た人が送れなくなるため。

設問はサイトの `suggest.html` と同じ並び・同じ必須条件:

| # | 質問 | 形式 | 必須 |
|---|---|---|---|
| 1 | 場所・作品の名前 | 記述式（短文） | 必須 |
| 2 | 公式ページのURL | 記述式（短文） | 任意 |
| 3 | 種類 | プルダウン（飲食・喫茶／体験・おでかけ／本・古書／音楽・ライブ／映画・演劇） | 任意 |
| 4 | どこが気になった？ | 段落 | 任意 |

## 回答の保存先

スプレッドシート「候補の受け取り｜フォーム回答」
id: `1QjLPCjyP5b5KpJflPxjcJBuJhbcAx4hI5ZIL2NFZWjg`

置き場所は `03_EDITORIAL_CONTENT_棚と作品 / 候補の受け取り_SUGGESTIONS`
https://drive.google.com/drive/folders/19u7ywNUNfh-0I3eimsIUyLq-WTWqQeYx

見出し行が設問と一致していることを確認済み:

```
タイムスタンプ | 場所・作品の名前 | 公式ページのURL | 種類 | どこが気になった？
```

作成直後はマイドライブ直下にできる。このフォルダに入っていないと週次の
見直しが見つけられないので、必ず移すこと。

## 適用済みの開示文

> このページでは保存・計測・個人ごとの推薦を行いません。入力内容は自動送信
> されません。「候補文をコピー」を押した場合だけ、端末のクリップボードに
> コピーされます。送信フォームと公式Xは、押したときだけ開きます。フォームは
> Google のページで、記入と送信はそちらで行います。

元の約束を1つも弱めず、行き先が増えたことだけを足している。
文言確定は独立監査の担当なので、codex 復帰後に確認を通す。

## prefill はしていない

フォームは空で開く。入力済みの文字を query に載せて渡す prefill は、押した
瞬間に文字が外部へ渡ることを意味し、「押したときだけ開きます」で説明できる
範囲を越える。二度書きの手間より、説明できることを優先した。
`release_check.js` が query 付きの form link を FAIL にする。

## 毎週やること

`候補の受け取り_SUGGESTIONS` のスプレッドシートを読み、次を判定する。

起動は当面 **Founder の声かけ**で行う（2026-08-29 決定）。「今週の候補見て」で
Claude がシートを読みに行く。

Routine による自動起動はまだ作らない。回答0件のシートを毎週読みに行く仕組みを
先に作っても、動くのは「何もありませんでした」という報告だけになる。候補が
実際に届き、週あたりの件数が見えてから作る方が、設定が無駄にならない。

作るときは claude.ai の Routines 画面から作る必要がある。**Claude Code の側から
作った Routine には Google Drive の connector が引き継がれず、起きても Drive を
読めない**（2026-08-29 に実際に作って確認し、無意味なので削除した）。
手順は 1) claude.ai の Routines で新規作成 2) スケジュールを週1回
3) connector に Google Drive を付ける（これが本体） 4) prompt に
`qa/SUGGESTION_WEEKLY_PROMPT.md` の中身をそのまま貼る。

1. 前回見た行より後の回答だけを見る
2. 各候補を編集契約に照らす
   - 事実として確認できるか（一次情報が公式ページにあるか）
   - `evergreen` にできるか。できないなら `mode: 'current'` として
     `verifiedAt` と `expiresAt` が引けるか
   - Hook が答えを先に言っていないか
   - 図版の権利が clear か。無ければ活字図版で組めるか
   - 既存12件と重複しないか
3. 通ったものを `qa/EDITORIAL_ROTATION.md` の候補欄へ書き足す
4. 判断の記録を Drive のフォルダへ残す

Claude はここまで。棚に載せる差し替えそのものは人の編集で行う。
`YOU MUST NOT merge to main without CEO approval` は毎週の運用でも変わらない。

## 送らないもの

このフォームで受け取るのは、公開されている場所・作品の推薦だけ。
利用者が書いた本文・タイトル・写真・バックアップの中身は、この経路に
一切乗らない。感情書店の本文は端末の中にあり、外へ出さない。
