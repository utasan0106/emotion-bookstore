# Development Run — 2026-08-27 16:50 JST

## Result

`CORE_UX_PATCH_GO`

Human Test remains `HOLD` for one reason only: exact Real Media must be localized same-origin before an external cycle.

## Product changes

1. Removed the visible `objectName` from pre-open cards.
   - Hachiko no longer leaks `剥製` before open.
   - Meguro no longer leaks `標本` before open.
2. Removed the external-link arrow from `ひらく` because it opens an in-page dialog.
3. Removed internal `verifiedNote` copy from participant-facing detail.
4. Demoted the legacy brand from the first expectation-setting position.
   - Frontstage: `東京の棚`
   - Byline: `by みんなの感情書店`
   - This is a reversible Pilot presentation decision, not a final corporate/product rename.
5. Replaced the abstract feature label with `「それ、本当にあるの？」な3つ`.
6. Simplified the primary moment copy to `まだ、行き先が決まっていない日に。東京を、3つだけ。`

## Automated gates added

`pilot_check.js` now fails if:
- pre-open participant UI renders `objectName`
- participant UI renders internal `verifiedNote`
- the dialog-open control is visually marked as an external link

Existing fail-closed checks remain for exactly 3 Objects, no search/form/account, no storage, no analytics, no fetch/XHR/beacon, HTTPS official actions, provenance/freshness, and finite ending.

## Browser QA

PASS at:
- 320×800
- 390×844
- 430×932
- 1024×768
- 1440×1000

All pass:
- 3 Objects exactly
- overflow 0
- first media region in initial viewport
- no pre-open Reveal spoilers
- dialog/focus/ESC/focus-return
- Reveal present after open
- internal audit-copy leak 0
- all six order permutations preserve the same Object set
- browser errors 0

## Media acquisition

Network download from the local execution sandbox is blocked. Drive image search did not find the three exact media as standalone reusable files. The process therefore did **not** substitute AI images, rights-unknown images, or Drive-rendered copies.

The exact-source localization helper remains ready. Human Test starts only after a network-capable build obtains the exact source bytes and records decode/dimensions/SHA-256/attribution.

## Canonical sync

Latest Tokyo Pilot handoff was updated with this run as a current override. Production/main remains untouched.
