# Tokyo Pilot 01 — Handoff / 2026-08-27 21:00 JST

Branch: `claude/tokyo-discovery-experience-gqfjuo`
Repository: `utasan0106/emotion-bookstore`

## 1. 今日完成したもの

1. **Pilot をリポジトリへ取り込んだ。** これまで zip の中にしか無かった
   Tokyo Pilot 01 を `v3-prototype/tokyo-pilot-01/` として git 管理下に置いた。
   前回 run で 403 だった GitHub への push は今日は通っている。
2. **参加者契約を実ブラウザで検証する QA harness（`qa/browser_qa.js`）。**
   320 / 390 / 430 / 1024 / 1440 と order permutation で 149 件を検査する。
   31 種類の検査があり、現在すべて PASS。
3. **Real Media を実寸比で出す runtime。** frame の比率を画像自身の実寸から取り、
   `mediaCrop` / `mediaCropNote` を content 側の宣言にした。
4. **期限切れの事実を参加者へ出さない fail-closed。** `expiresAt` を過ぎた Object が
   あれば参加者モードは棚を描かない。`pilot_check.js --external-cycle` は FAIL する。
5. **Human Test 手順を機械 gate 中心に書き直した。** 12 項目を3コマンドに畳み、
   人がやるしかない 1 項目（一次情報の再確認）だけを独立させた。
6. **operator 向け README と DECISION NEEDED 記録。**

## 2. 今日削ったもの

- hero 下の特集チップ（今の棚 /「それ、本当にあるの？」な3つ）
- 一覧カードの種別・地名メタ行（`カフェ・作業場所 · 高円寺三角地帯 / 高円寺`）
- footer 1文目（東京の棚は…3つだけ仕入れます）
- 「科博」という内輪の略称
- 1件目カードだけを持ち上げていた装飾、目黒だけの `contain` 対症、
  参照元の消えた `.feature-chip` / `.section-heading` CSS
- 詳細 media の灰色 letterbox 帯
- 実体の無くなった `APPLY_TO_REPO.md` / `PACKAGE_MANIFEST.txt`

## 3. UX 上、なぜ改善なのか

**Real Media が初めて Hook として機能する。** 原稿執筆カフェは縦位置のポスターを
横フレームに `cover` していたため、実測で card 46〜57% / 詳細 39〜53% しか
見えていなかった。円形ロゴも店名も切れており、Real Media が First Pull を
担えていなかった。目黒も横フレームに `contain` して灰色の中の細い帯になり、
8.8m という長さの証拠が読めなかった。いまはどちらも実寸比で全景が出る。

**payoff が一番大きい要素になった。** 詳細では既読の Hook が clamp(30〜50px)、
未知の Reveal が clamp(20〜28px) で、一番大きい要素が一番情報量の少ない要素だった。
Hook を小さい echo に降ろし、Reveal を見出しにした。

**説明を読ませる段が3つから1つになった。** Object の手前に kicker / h1 / 特集チップの
3段があった。「それ、本当にあるの？」という評価は Object 自身が出すべきもので、
UI が先に宣言していた。いまは1行だけが Object の手前にある。

**First Pull が測定対象になった。** どの Object が1件目に来ても
（abc / bac / cab）、5つの viewport すべてで Real Media + Hook 全文 +「ひらく」が
スクロールなしで見えることを実測している。

**古い営業情報が参加者に届かなくなった。** 「外部サイクル前に必ず再確認」は
運用文でしか担保されていなかった。

## 4. PASS した QA

```
node pilot_check.js                  → PILOT_CHECK_GO / FRESHNESS_OK
node pilot_check.js --external-cycle → PILOT_CHECK_GO
python media_validate.py             → MEDIA_VALIDATE_GO（3件の実バイト・寸法・SHA-256 一致）
node qa/browser_qa.js                → BROWSER_QA_GO (149/149)
node --check pilot.js / pilot_content.js / pilot_check.js / qa/browser_qa.js → PASS
python -m py_compile media_validate.py localize_media.py → PASS
git diff --check → clean
```

browser QA が実測している 31 種類:
media decode / 遅延なし読み込み / crop identity（card・detail）/
1画面目の Media+Hook・ひらく・有限カウンタ / 横 overflow 0 /
一覧のネタバレ 0・内部語 0・objectName 0 / ひらくが button であること /
dialog 開閉・Reveal 優位・overflow・HTTPS+noopener・内部語 0・focus /
Escape で閉じる・focus がトリガーへ戻る / 有限な終わり /
storage 書き込み 0・cookie 0 / fetch・XHR・sendBeacon 0 / 外部 request 0 /
order 3通りの First Pull / 参加者モードの正常描画・期限切れ停止・停止画面の内部語 0 /
6 order の identity 一致

**新規 FAIL 0。** 取り込み時点の baseline（`qa/qa-baseline-20260827.json`、
101/116）にあった FAIL 2 系統は解消済み。
静的 gate は意図的に壊して FAIL することを確認している
（固定 aspect-ratio への差し戻し / 宣言寸法と証跡のずれ / 期限切れ /
参加者 halt の削除）。

## 5. 残っている FAIL / blocker

**FAIL は 0。blocker は 1 件。**

**BLOCKER: 一次情報の再確認ができていない（Human Test 前提条件 13）。**
本開発環境の network egress proxy が
`kiseichu.org` / `koenji-sankakuchitai.blog.jp` / `db.kahaku.go.jp` /
`commons.wikimedia.org` をすべて遮断しており、公式一次情報を1件も読めなかった
（curl / WebFetch とも到達不可）。掲載事実は 2026-08-27 17:16 JST 時点の
既存記録のままで、今日の再確認は行えていない。
原稿執筆カフェの `expiresAt` は `2026-08-30T16:00:00+09:00`。

## 6. Human Test を開始できるか

**機械 gate は全部 GO。ただし、まだ開始してはいけない。**

前提条件 13（一次情報の再確認）が未達のため。これは外部ネットワークに
到達できる環境から実行する必要がある。再確認が済めば、技術的にはそのまま
Cycle 01 を回せる。D-03（参加者導線）と D-04（Preview 配信面）も未決。

## 7. Production へ進めない理由

- 一次情報の再確認が未実施
- 行動データが 0。Object Open Rate も Return Desire もまだ観測していない
- runtime media 3件は Human Test 限定の technical derivative であり、
  `production_promotion: false`。Production の media / legal / quality gate を
  通っていない
- main merge / Production deploy / Primary domain 切替は委任範囲外

`main` は無変更。CSP / GA4 / storage / registry / V2 / V3 本体も無変更。

## 8. Founder が判断すべきこと（最大3件）

1. **D-01: 目黒の Reveal に宿主の事実を入れるか。** いま payoff が一番弱い。
   一次情報で「1986年に人から駆虫された」ことを確認できれば大きく効くが、
   二次情報しか無い状態で載せるかどうかは方針判断。
2. **D-03: 12〜18名の参加者をどこから集めるか。** 有償インセンティブや
   ベンダー契約が必要なら委任範囲外。
3. **D-04: Cycle 01 をどの Preview URL で回すか。**

（`DECISIONS_NEEDED.md` に D-02 を含む全件を記録している。）

## 9. 最新 HEAD / branch / files / package

- branch: `claude/tokyo-discovery-experience-gqfjuo`（origin と一致）
- base: `eca334f9671bee07833892b2476aac118f8ed018`
- 変更範囲: `v3-prototype/tokyo-pilot-01/**` と `.vercelignore` のみ

runtime（参加者が読む7件）:
`index.html` / `pilot.css` / `pilot_content.js` / `pilot.js` /
`assets/manuscript-cafe.png` / `assets/hachiko.jpg` / `assets/meguro-tapeworm.jpg`

gate・証跡・手順:
`pilot_check.js` / `media_validate.py` / `qa/browser_qa.js` /
`MEDIA_LOCALIZATION_EVIDENCE.json` / `MEDIA_ATTRIBUTION.md` /
`MEDIA_LOCALIZATION_READY.md` / `HUMAN_TEST_CYCLE_01.md` /
`HUMAN_TEST_SCORECARD.csv` / `README.md` / `DECISIONS_NEEDED.md` /
`qa/qa-report.json` / `qa/qa-baseline-20260827.json`

`.vercelignore` は上記 gate・証跡・手順を配信面から除外し、
参加者が読む7件だけを残す。`media_validate.py` は Pillow が必要。

## 10. 次に再開するときの最優先1件

**外部ネットワークに到達できる環境から、3つの公式一次情報を再確認し、
`verifiedAt` / `expiresAt` / facts を更新して
`node pilot_check.js --external-cycle` を GO にすること。**

これが Human Test 開始の唯一の残り条件であり、
原稿執筆カフェの `expiresAt` が 2026-08-30 16:00 JST で切れるため期限がある。
UI をこれ以上磨くのは、行動データが出るまで優先度が低い。
