# Tokyo Pilot 01 — Development Run Report

Run: 2026-08-27 / 17:00 JST block

## Decision

**TECHNICALLY READY FOR ISOLATED HUMAN TEST / PRODUCTION HOLD**

The previous same-origin media blocker has been cleared without introducing runtime hotlinks, unknown-rights substitutes, AI imagery, storage, analytics, accounts, or recommendation runtime.

## What changed

1. Removed pre-open Object names that spoiled Reveal answers.
2. Removed external-link semantics from the in-page `ひらく` control.
3. Removed internal verification notes from participant UI.
4. Moved frontstage presentation to `東京の棚` with `みんなの感情書店` as a quiet byline for the Pilot.
5. Localized all 3 source-pinned media as same-origin Human-Test assets.
6. Added machine-readable media evidence with rights/source/runtime hashes and explicit Human-Test-only scope.
7. Added fail-closed media validation and strengthened static Pilot checks.
8. Real-media Browser QA passed at 320/390/430/1024/1440.
9. Real-media review found and fixed Meguro evidence-destroying crop; card/detail now use full-frame `contain`.
10. Human Test protocol updated to TECHNICALLY READY; external participant recruitment/test has not started.
11. Added an anonymous 18-row Human Test scorecard with the six counterbalanced orders.

## Validation

- `node tokyo-pilot-01/pilot_check.js` → GO
- `python tokyo-pilot-01/media_validate.py` → GO
- JS syntax checks → PASS
- Python compile checks → PASS
- Real-media browser QA → GO, 5 viewports
- 3 images decode, no horizontal overflow, spoiler leak 0, internal-note leak 0, dialog/focus/Escape/focus-return PASS

## Media scope

The Commons images are proportional technical derivatives generated by a Google source-fetch/export bridge because direct source download is blocked in the local runtime. Full frame is retained and the transform is documented. Cafe preserves source dimensions.

All three are **Human Test only** and `production_promotion=false`.

## Still protected

No change to:
- Production main
- CSP
- GA4
- storage/registry contracts
- primary domain
- payment/contracts

GitHub remote write remains blocked by integration-level HTTP 403; main remains untouched.

## Next product action

Do not add features. Run the 12–18 person isolated first-time-user test when a no-contract participant route is available. The next Product decision must be based on Object Open Rate and Return Desire, not visual polish or feature requests.

## Freshness correction at 17:16 JST

A new official-source recheck superseded the older Cafe 9/25 snapshot for the current runtime. The currently verifiable official page lists scheduled operation through 2026-08-30 16:00 and says later dates are adjusting, so `expiresAt` is now conservatively `2026-08-30T16:00:00+09:00`.

Hachiko and Meguro current facts remain supported by their official sources. All three must still be reverified immediately before an external test cycle.
