# V3 User-First Single Bet — 16:00 development report

## Decision held

No scope expansion. The Pilot remains `検索前の3分 / 東京を3つだけ`, with exactly three objects and no emotion-first entry, search/catalog, AI recommendation, account, save/history, ranking, infinite feed, analytics, or storage.

## Work completed

1. Re-read the current Drive canonical and independently re-checked the GitHub base branch. Branch HEAD is still `57c7e2e712439bf3f2a8bb0b16720d735b497a54`; GitHub write retry still fails with 403, so remote/main/Production remain untouched.
2. Reverified the three selected objects against current primary/rights sources.
   - 原稿執筆カフェ: official page currently states media may use page photos/text, limits use to manuscript/creative work, requires an entry goal, blocks checkout while the goal is unfinished during business hours, and lists current Aug/Sep 2026 schedule.
   - Hachiko: National Museum permanent exhibition DB still lists `秋田犬（ハチ）`, specimen type `剥製`, Japan Gallery 2F North Wing. Exact Commons image remains Momotarou2012 / CC BY-SA 3.0.
   - Meguro: official museum currently lists 10:00–17:00, Mon/Tue closures with holiday exception, free admission; official history records the 8.8m Japanese broad tapeworm was expelled and display began in 1986. Exact Commons media remains Laika ac / CC BY-SA 2.0.
3. Tightened first-fold UX. Initial 390px Browser QA showed too much explanatory space before Real Media. Removed the hero lede, reduced hero vertical space, simplified the section heading, and kept the editorial feature title without adding functionality.
4. Tightened Reveal copy:
   - Cafe: `入店時に目標を書き、達成するまで精算できない。進捗チェックもある。`
   - Hachiko: unchanged paired-pass copy.
   - Meguro: `1986年に駆虫。今も、その8.8mを標本で見られる。`
   These are current PO Pilot copy choices; no past Founder rating was rewritten.
5. Hardened `pilot_check.js`: exactly 3, unique IDs, Hook <= 28 chars, Reveal <= 60 chars, finite ending, no search/form input, no storage/analytics/background API, HTTPS action/rights URLs, provenance/license/verifiedAt/reverify fields, responsive and reduced-motion CSS checks.
6. Added fail-closed `localize_media.py` plus `MEDIA_LOCALIZATION_READY.md`. In a network-capable isolated branch it will acquire the three frozen assets, validate source image constraints, generate same-origin runtime files, and only then switch content from external-preview URLs to local assets.

## Verification

- `node pilot_check.js` => GO after the changes.
- `python -m py_compile localize_media.py` => PASS.
- Browser re-run in this runtime is blocked by administrator policy for localhost/file navigation, so the fold refit is not being claimed as browser-PASS yet. Prior interaction QA remains evidence for the pre-refit structure only.
- Actual Real Media same-origin rendering remains the next technical gate before external Human Test.

## No-go areas unchanged

No main merge, Production deploy, primary-domain cutover, payment/contract, external paid work, private-data growth loop, or external Human Test.
